@model Tuple<List<Models.AFBAlliance>, List<Models.AFBAlliance>, int>
@{
    Layout = null;
    string viewName = Model.Item3 == 1 ? "AllianceAdd" : (Model.Item3 == 2 ? "AllianceDelete" : "AllianceEdit");
    List<Models.AFBAlliance> afb = new List<Models.AFBAlliance>();
    int index = 0;
}
@if (Model.Item2.Any())
{
    foreach (var newitem in Model.Item2)
    {
        if (Model.Item1.Any())
        {
            var olditem = Model.Item1[index];
            afb.Add(olditem);
        }
        index++;
        afb.Add(newitem);
        @Html.DisplayFor(p => afb, viewName)
        afb.Clear();
    }
}
else
{
    if (Model.Item1.Any())
    {
        foreach (var newitem in Model.Item1)
        {
            afb.Add(newitem);
            @Html.DisplayFor(p => afb, viewName)
            afb.Clear();
        }
    }
}
