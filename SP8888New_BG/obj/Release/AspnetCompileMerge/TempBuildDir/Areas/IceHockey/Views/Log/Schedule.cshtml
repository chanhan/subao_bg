@model Tuple<List<Models.ViewModel.IceHockey>, List<Models.ViewModel.IceHockey>, int>
@{
    Layout = null;
    string viewName = Model.Item3 == 1 ? "SchedulesAdd" : (Model.Item3 == 2 ? "SchedulesDelete" : "SchedulesEdit");
    List<Models.ViewModel.IceHockey> ice = new List<Models.ViewModel.IceHockey>();
    int index = 0;
}
@if (Model.Item2.Any())
{
    foreach (var newitem in Model.Item2)
    {
        if (Model.Item1.Any())
        {
            var olditem = Model.Item1[index];
            ice.Add(olditem);
        }
        index++;
        ice.Add(newitem);
        @Html.DisplayFor(p => ice, viewName)
        ice.Clear();
    }
}
else
{
    if (Model.Item1.Any())
    {
        foreach (var newitem in Model.Item1)
        {
            ice.Add(newitem);
            @Html.DisplayFor(p => ice, viewName)
            ice.Clear();
        }
    }
}
